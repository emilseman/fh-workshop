{
  "name": "fh-workshop",
  "description": "Sample project for feature hub bootstrap",
  "main": "index.js",
  "author": "Emil Seman <emil.seman@sinnerschrader.com>",
  "license": "MIT",
  "scripts": {
    "build": "lerna run build",
    "watch": "lerna run watch --parallel",
    "format": "prettier --single-quote --trailing-comma all --write \"{{**/src,__{tests,mocks}__}/**/*.{ts,tsx,js},**/webpack.config.js}\""
  },
  "devDependencies": {
    "lerna": "^3.20.2",
    "prettier": "^1.19.1"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ]
}
